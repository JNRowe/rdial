language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
  - 3.5
install:
  - pip install -r extra/requirements.txt
  - pip install -r extra/requirements-test.txt
script:
  - nose2 tests
branches:
  except:
    - /^play\/.*$/
