language: python
python:
  - 2.6
  - 2.7
install:
  - pip install --use-mirrors coverage expecter nose2 nose2_cov
  - pip install --use-mirrors .
script:
  - nose2 -v tests
branches:
  except:
    - /^play\/.*$/
