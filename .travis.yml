language: python
python:
  - 2.6
  - 2.7
install:
  - pip install --use-mirrors behave coverage nose
  - pip install --use-mirrors .
script:
  - nosetests -v tests
  - behave
branches:
  except:
    - /^play\/.*$/
